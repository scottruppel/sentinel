# Optional: copy this file to ``backend/.env`` (same folder as this example).
# If ``backend/.env`` does not exist, SENTINEL also loads the repo-root ``.env``
# (one directory above ``backend/``). Use either location; do not commit real secrets.

# See the repository ``.env.example`` at the project root for full variable list.
