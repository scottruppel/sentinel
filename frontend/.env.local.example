# Demo mode: laptop frontend → VPS backend via Tailscale
# Copy this file to .env.local and fill in the real key from the VPS .env
VITE_API_TARGET=http://100.111.177.67:8002
VITE_API_KEY=<SENTINEL_API_KEY from VPS .env>
